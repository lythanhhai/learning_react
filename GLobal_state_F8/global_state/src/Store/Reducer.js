
const initState = {
    todos: [],
    todoInput:'',
}

// const set_todo = 'set_todo';

// const add_todo = 'add_todo';

// const remove_todo = 'add_todo';
 

const reducer = (state, action) => {
    switch(action.type)
    {
        

    }
}

export { initState };

export default reducer;